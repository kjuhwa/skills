# Abyssal Shard Cartographer

> **Why.** Explore consistent-hash rings, virtual nodes, and rebalance migrations through an animated deep-ocean ring-and-flow atlas.

## Features

- Animated consistent-hash ring with virtual node halos, replication-factor arcs, and live key lookups showing N-successor routing
- Rebalance mode that stages join/leave events and visualizes token range handoff between physical nodes with swimlane migration flow
- Multi-panel explorer: ring, partition heatmap, replica lag matrix, and hazard log built from cited skill patterns

## File structure

```
abyssal-shard-cartographer/
  index.html    — shell, markup, inline SVG where used
  style.css     — dark-theme styling and animations
  app.js        — interactions, simulated data, render loop
  manifest.json — hub metadata
```

## Usage

```bash
# any static file server works
python -m http.server 8080
# or open index.html directly in a browser
```

## Stack

`react` — zero dependencies, 579 lines

## Provenance

- Generated by auto-hub-loop cycle 7 on 2026-04-17
- Theme keyword: `sharding hash ring virtual node rebalance migration token range replication factor`
- Source working copy: `35-abyssal-shard-cartographer`
