# Inkwell Constellation Composer

> **Why.** A keyword-fed tool that forges poetic constellation names, SVG star charts, and exportable lore cards.

## Features

- Multi-strategy generator registry (`pluggable-sender-factory-pattern`, `strategy-spi-list-to-map-autoinject`, `resource-provider-registry`).
- Byte-safe truncation and hash-suffix naming per `byte-aware-sms-truncation-with-ellipsis` and `identifier-truncate-with-hash-suffix`.
- Preset save/load, robust JSON extraction, and jittered regeneration via `zustand-preset-manager`, `llm-json-extraction`, `cache-variance-ttl-jitter`.

## File structure

```
inkwell-constellation-composer/
  index.html    — shell, markup, inline SVG where used
  style.css     — dark-theme styling and animations
  app.js        — interactions, simulated data, render loop
  manifest.json — hub metadata
```

## Usage

```bash
# any static file server works
python -m http.server 8080
# or open index.html directly in a browser
```

## Stack

`html` · `css` · `vanilla-js` — zero dependencies, 252 lines

## Provenance

- Generated by auto-hub-loop cycle 1 on 2026-04-17
- Theme keyword: `Velvet cartographers chart forgotten constellations across drifting paper seas where inkwell whales surface beneath lantern-lit skies`
- Source working copy: `19-inkwell-constellation-composer`
